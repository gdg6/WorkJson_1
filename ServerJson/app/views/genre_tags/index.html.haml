%h1 Listing genre_tags

%table
  %thead
    %tr
      %th Title
      %th
      %th
      %th

  %tbody
    - @genre_tags.each do |genre_tag|
      %tr
        %td= genre_tag.title
        %td= link_to 'Show', genre_tag
        %td= link_to 'Edit', edit_genre_tag_path(genre_tag)
        %td= link_to 'Destroy', genre_tag, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Genre tag', new_genre_tag_path
