%h1 New genre_tag

= form_for @genre_tag do |f|
  - if @genre_tag.errors.any?
    #error_explanation
      %h2= "#{pluralize(@genre_tag.errors.count, "error")} prohibited this genre_tag from being saved:"
      %ul
        - @genre_tag.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :title
    = f.text_field :title
  .actions
    -#= f.submit 'Save'
    =link_to 'save', '#', :class => 'sys-add-genre_tag'

= link_to 'Back', genre_tags_path
