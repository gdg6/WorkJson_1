= form_for @genre_user_tag do |f|
  - if @genre_user_tag.errors.any?
    #error_explanation
      %h2= "#{pluralize(@genre_user_tag.errors.count, "error")} prohibited this genre_user_tag from being saved:"
      %ul
        - @genre_user_tag.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :title
    = f.text_field :title
  .actions
    = f.submit 'Save'
